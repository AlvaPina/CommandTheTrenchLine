export default class AI {
    constructor(scene) {
        this.scene = scene;
        this.inputDelay = 10000; // Cooldown en milisegundos
        this.canInteract = true;
    }

    update() {
   
    }
}
